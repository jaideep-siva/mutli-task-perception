# Data Package

This package contains the canonical manifest-driven dataset and collate function for multitask training.

Large local images, masks, manifests, and generated datasets can still live under `data/`, but they are ignored by Git by default.
