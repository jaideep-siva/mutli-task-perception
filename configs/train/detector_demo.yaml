epochs: 10
lr: 0.0001
weight_decay: 0.0001
device: auto
amp: true
log_every: 10
vis_every_epochs: 1
score_thresh: 0.5
seed: 42
checkpoint_dir: outputs/checkpoints
tb_dir: outputs/tensorboard
pred_dir: outputs/predictions
